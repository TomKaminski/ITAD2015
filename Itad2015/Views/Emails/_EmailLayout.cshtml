@RenderSection("Headers", false)
Content-Type: text/html; charset=utf-8

<html>
<head>
    <meta name="format-detection" content="telephone=no">
    <meta name="viewport" content="width=device-width">

</head>
<body mail="fix">
    <div style="width: 700px; text-align:center">
        <a href="@Url.Action("Index","Home",null,"http")" style="margin-bottom:35px">
            <div style="width: 700px; height: 350px">
                @Html.EmbedImage("~/Content/images/Mail/mailHeader.png")
            </div>
        </a>
        @RenderBody()
        <a href="@Url.Action("Index","Home",null,"http")" style="margin-top:35px">
            <div style="width: 700px; height: 350px">
                @Html.EmbedImage("~/Content/images/Mail/mailFooter.png")
            </div>
        </a>
    </div>

</body>
</html>